/** Still images have no intrinsic duration: they get a default span when placed
 * and can be trimmed up to a generous cap (treated as their "source length"). */
export const DEFAULT_IMAGE_DURATION = 5
export const IMAGE_MAX_DURATION = 600
